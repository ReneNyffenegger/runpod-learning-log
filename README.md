# runpod learning log

[`API`](API) collects a few `curl` commands to demonstrate the [runpod.io rest API](https://docs.runpod.io/api-reference/overview).

This [Hello world](serverless/hello-world) creates a simple endpoint, runs a job on it and displays the job's result.

[Flash](flash)

[SDK](sdk)
