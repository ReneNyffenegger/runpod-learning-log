A *container registry auth* stores the credentials necessary to connect to a private
Docker registry which are potentially referenced in a template.

[`list`](list) lists the configured authentications.

[`create`](create) creates a new password for [my docker hub](https://hub.docker.com/u/renenyffenegger).
